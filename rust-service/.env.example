# Rust microservice
PORT=8080
RUST_LOG=rust_service=debug

# Existing Node.js backend (must be running)
NODE_API_BASE_URL=http://localhost:5007

# Service account used to call the protected Node student endpoints.
# This is the seeded demo admin from seed_db/seed-db.sql, not a real credential.
NODE_API_USERNAME=admin@school-admin.com
NODE_API_PASSWORD=3OU4zn3q6Zh9
